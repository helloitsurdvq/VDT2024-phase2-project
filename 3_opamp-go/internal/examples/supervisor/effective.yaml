exporters:
  otlphttp/own_metrics:
    metrics_endpoint: http://localhost:4318/v1/metrics
extensions:
  health_check: null
receivers:
  prometheus/own_metrics:
    config:
      scrape_configs:
      - job_name: otel-collector
        scrape_interval: 10s
        static_configs:
        - targets:
          - 0.0.0.0:8888
service:
  extensions:
  - health_check
  pipelines:
    metrics/own_metrics:
      exporters:
      - otlphttp/own_metrics
      receivers:
      - prometheus/own_metrics
  telemetry:
    logs:
      encoding: json
    resource:
      service.instance.id: 01916e1f-58e8-7156-b333-28a893806ff1
      service.name: io.opentelemetry.collector
      service.version: 1.0.0
